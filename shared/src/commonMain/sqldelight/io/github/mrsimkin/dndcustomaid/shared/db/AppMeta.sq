CREATE TABLE app_meta (
    key TEXT NOT NULL PRIMARY KEY,
    value TEXT NOT NULL
);

upsert:
INSERT OR REPLACE INTO app_meta(key, value)
VALUES (?, ?);

findValueByKey:
SELECT value
FROM app_meta
WHERE key = ?;
