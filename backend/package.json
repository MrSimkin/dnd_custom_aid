{
  "name": "dnd-custom-aid-backend",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "wrangler types && tsc --noEmit && node --test",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@neondatabase/serverless": "1.1.0",
    "jose": "6.2.12"
  },
  "devDependencies": {
    "typescript": "6.0.3",
    "wrangler": "4.127.1"
  }
}
