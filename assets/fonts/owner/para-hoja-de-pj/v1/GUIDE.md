# Para Hoja de PJ v1 — archival original and use guide

Status: **immutable archival original**.

This is the exact owner-authored 2007 `Para Hj De Pj.ttf` supplied by Gustavo Muñoz and verified before repository publication. It must remain byte-for-byte unchanged so later versions have trustworthy provenance.

## Binary identity

- repository file: `Para Hj De Pj.ttf`;
- size: **12,744 bytes**;
- SHA-256: `d0c0d50ad8ed33064e3af89b0976933b9eba8ead2d26234011c1a3233c246658`;
- Git blob: `67cf8eb51dbcee7b4c68c8ef7e9e238ce010271c`;
- original internal family/name metadata: historical FontCreator-era data, preserved as-is;
- units-per-em: `2048`;
- historical OS/2 embedding flag: `fsType = 8`, preserved as part of the original binary rather than silently modernized.

Owner authorization permits repository publication and derivative modernization. That authorization does **not** mean v1 should be technically rewritten; modernization belongs in later version folders.

## Historical keyboard mappings

| Keyboard key | Unicode | Historical symbol meaning | Relationship to later versions |
| --- | --- | --- | --- |
| `A` / `a` | `U+0041` / `U+0061` | round outline | Meaning is preserved on the same keys in v4; geometry may be redesigned |
| `B` / `b` | `U+0042` / `U+0062` | square outline | Meaning is preserved on the same keys in v4; geometry may be redesigned |
| `C` / `c` | `U+0043` / `U+0063` | narrow oval outline | Meaning is preserved on the same keys in v4; geometry may be redesigned |
| `D` / `d` | `U+0044` / `U+0064` | narrow oval filled | Meaning is preserved on the same keys in v4; geometry may be redesigned |
| `E` / `e` | `U+0045` / `U+0065` | square filled | Meaning is preserved on the same keys in v4; geometry may be redesigned |

The legacy uppercase/lowercase pairs above have the same historical meaning. Do not retroactively reinterpret v1 according to the newer lowercase-outline / uppercase-filled convention used by additional v4 paired symbols.

## Archival rule

Do not reconstruct, regenerate, normalize or substitute this binary. Any repair, metadata modernization, Unicode/PUA expansion, spacing change or glyph redesign must create a later version and document its relationship to v1.
