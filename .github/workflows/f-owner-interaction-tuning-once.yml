name: F owner interaction tuning once

on:
  push:
    paths:
      - '.github/workflows/f-owner-interaction-tuning-once.yml'

permissions:
  contents: write

jobs:
  tune:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'
      - uses: android-actions/setup-android@v4
        with:
          packages: 'platform-tools platforms;android-36'
          log-accepted-android-sdk-licenses: 'false'
      - uses: gradle/actions/setup-gradle@v6
        with:
          gradle-version: '9.5.0'
      - name: Apply guarded interaction tuning
        run: python3 .github/f-owner-interaction-tuning-once.py
      - name: Check patch hygiene
        run: git diff --check
      - name: Prepare stable CI debug keystore
        run: base64 --decode androidApp/ci-debug.keystore.b64 > androidApp/ci-debug.keystore
      - name: Verify interaction checkpoint
        run: gradle :shared:desktopTest :androidApp:compileDebugKotlin --stacktrace
      - name: Commit interaction tuning and self-remove
        shell: bash
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          rm .github/workflows/f-owner-interaction-tuning-once.yml .github/f-owner-interaction-tuning-once.py
          git add -A
          git commit -m 'fix: tune spell reorder feel for owner retest'
          git push
