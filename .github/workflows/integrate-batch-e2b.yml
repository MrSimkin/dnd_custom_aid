name: One-time Batch E2b class identity integration

on:
  push:
    branches:
      - implementation/phase4-character-closure
    paths:
      - .github/workflows/integrate-batch-e2b.yml

permissions:
  contents: write

jobs:
  integrate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: implementation/phase4-character-closure
          fetch-depth: 0
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - name: Apply guarded E2b integration
        run: python scripts/integrate_batch_e2b.py
      - name: Remove one-time scaffolding
        run: |
          rm scripts/integrate_batch_e2b.py
          rm .github/workflows/integrate-batch-e2b.yml
      - name: Verify whitespace
        run: git diff --check
      - name: Commit E2b integration
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "feat: integrate Batch E2b class identity editor"
          git push origin HEAD:implementation/phase4-character-closure
