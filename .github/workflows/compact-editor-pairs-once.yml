name: Compact editor pairs once

on:
  push:
    paths:
      - '.github/workflows/compact-editor-pairs-once.yml'

permissions:
  contents: write

jobs:
  compact:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'
      - uses: android-actions/setup-android@v4
        with:
          packages: 'platform-tools platforms;android-36'
          log-accepted-android-sdk-licenses: 'false'
      - uses: gradle/actions/setup-gradle@v6
        with:
          gradle-version: '9.5.0'
      - name: Run guarded editor density migration
        run: python3 .github/compact-editor-pairs-once.py
      - name: Prepare stable CI debug keystore
        run: base64 --decode androidApp/ci-debug.keystore.b64 > androidApp/ci-debug.keystore
      - name: Compile compact editor density pass
        run: gradle :androidApp:compileDebugKotlin --stacktrace
      - name: Commit density pass and self-remove
        shell: bash
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          rm .github/workflows/compact-editor-pairs-once.yml
          rm .github/compact-editor-pairs-once.py
          git add androidApp/src/main/kotlin/io/github/mrsimkin/dndcustomaid/android .github/workflows/compact-editor-pairs-once.yml .github/compact-editor-pairs-once.py
          git commit -m 'fix: compact related editor fields across character UI'
          git push
