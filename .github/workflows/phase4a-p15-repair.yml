name: Phase4A P15 surgical repair

on:
  push:
    branches:
      - implementation/phase4a-successor-cycle
    paths:
      - .github/workflows/phase4a-p15-repair.yml
  workflow_dispatch:

permissions:
  contents: write

jobs:
  repair:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: implementation/phase4a-successor-cycle
          fetch-depth: 0
      - name: Apply audited P15 repair
        shell: bash
        run: |
          python3 .github/scripts/phase4a_p15_repair.py
          python3 -c 'from pathlib import Path; paths=[Path("androidApp/src/main/kotlin/io/github/mrsimkin/dndcustomaid/android/CharacterSupercompactV4.kt"),Path("androidApp/src/main/kotlin/io/github/mrsimkin/dndcustomaid/android/CharacterEditorV4.kt")]; [p.write_text(p.read_text().rstrip()+"\n") for p in paths]'
      - name: Validate and commit
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --check
          git diff --cached --stat
          git commit -m "repair: close P15 supercompact projection gaps"
          git push origin HEAD:implementation/phase4a-successor-cycle
