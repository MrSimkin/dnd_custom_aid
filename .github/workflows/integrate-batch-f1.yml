name: One-time Batch F1 inventory foundation

on:
  push:
    branches:
      - implementation/phase4-character-closure

permissions:
  contents: write

jobs:
  integrate:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      - uses: actions/setup-python@v6
        with:
          python-version: '3.13'
      - name: Apply guarded F1 patch
        run: python .github/scripts/apply_batch_f1.py
      - name: Remove one-time scaffolding
        run: rm .github/scripts/apply_batch_f1.py .github/workflows/integrate-batch-f1.yml
      - name: Verify whitespace
        run: git diff --check
      - name: Commit F1 integration
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'feat: add Batch F1 inventory foundation'
          git push
