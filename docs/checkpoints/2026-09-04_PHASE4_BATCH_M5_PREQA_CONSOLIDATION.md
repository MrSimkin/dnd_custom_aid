# Phase 4 Batch M5 — pre-QA consolidation

**Date:** 2026-09-04  
**Status:** GREEN / EXACT REPLACEMENT QA CANDIDATE FROZEN  
**Safety branch:** `tmp/phase4-m5-consolidation`  
**Authoritative M4 base:** `87e873fa7b9cdcb5c61b1d4ac78a71dd5ad380c2`  
**Helper-free M5 product commit:** `b5145a1e0ae92bb7594b9b650f8849eb51f85ba2`  
**Helper-free product tree:** `4a145ffcbed7bb176ff137f0dada41b6f4f3e2a0`  
**Exact frozen QA candidate branch:** `tmp/phase4-m5-frozen-qa-candidate`  
**Exact frozen candidate commit:** `adc286b3e1305ed706c2ed04d478a43652f6b365`  
**Exact frozen candidate tree:** `fd1f7feffde082b34cce41248e951a25eed7a004`  
**Canonical `main`:** untouched  
**Historical L candidate:** untouched / historical evidence only

## Purpose

M5 is the final automated pre-QA consolidation stage after M4 closed the six inter-batch D-0047 scope holes. It had four responsibilities:

1. re-run implementation traceability for all six M4 repairs;
2. perform only the bounded code-health cleanup justified by M2 and current compiler evidence;
3. re-run the full automated regression/build gate including owner-lineage migration coverage;
4. freeze and independently validate one new exact candidate for M6 owner real-device QA.

All four responsibilities are complete.

## M4 traceability re-audit — PASS

The guarded M5 cleanup gate re-asserted reachable implementation for all six holes identified by M1/M3:

- **F14:** structured proficiency/language management remains wired through `CharacterProficienciesCardV4` and the durable proficiency draft;
- **F15:** generic Resources retain Favorite/Quick Access management through `CharacterQuickAccessKind.RESOURCE`;
- **I18:** character-list projection retains class/subclass summary, freshness and optional portrait reference;
- **I21:** Application Settings retains the miniature real-sheet preview;
- **D06:** the active class/subclass identity surface reaches `CharacterRulesSourceBadgesV4`, while the shared primitive retains explicit `RULES` and `SOURCE` kinds;
- **D07:** real Spells, Equipment and Gestión concentration surfaces retain shared `STATE` badges and Favorite remains text/symbol accessible rather than color-only.

No schema or persistence migration was added by M4 or M5.

## Bounded M2 cleanup — COMPLETE

The cleanup was deliberately limited to code that the M2 audit identified as superseded/dead and that a fail-closed symbol scan proved had no active external references.

Removed complete superseded Android surfaces:

- `CharacterEquipmentTabV4.kt` — superseded by `CharacterEquipmentClosureTabV4`;
- `CharacterTraitsTabV4.kt` — superseded by `CharacterTraitsClosureTabV4`;
- `CharacterSpellListV4.kt` — superseded by `CharacterSpellListClosureV4`;
- `CharacterDomainShellsV4.kt` / `CharacterDomainShellV4` — placeholder shell with no active route.

Removed from `CharacterEditorV4.kt`:

- obsolete `classNamesV4` list;
- obsolete private `StatusSelectorV4` / `ClassesCardV4` / `ClassRowV4` / `ClassSelectorV4` / `HitDieSelectorV4` chain, superseded by the current class-identity and PC Settings surfaces.

The authoritative M4 -> M5 product comparison contains only the intended Android cleanup plus the four warning-level edits below. No generated backend lockfile remains.

## Compiler warning cleanup — COMPLETE

The first successful cleanup build exposed four bounded Kotlin warnings suitable for safe pre-QA repair. They were fixed and then explicitly asserted absent in a second full regression gate:

- unnecessary safe-call in subclass identity mode resolution;
- impossible Elvis fallback for validated equipment quantity;
- redundant always-true `selectedSourceId` condition in the spell Prepared control;
- deprecated Material 3 `ScrollableTabRow` replaced with `PrimaryScrollableTabRow` for the primary character tab strip.

An incidental `backend/package-lock.json` generated by `npm install` during the first guarded gate was proven absent from the authoritative M4 base and removed. Subsequent backend verification used `npm install --no-package-lock` for this stage.

## Explicitly deferred code-health items

M5 does **not** perform a broad architecture rewrite.

The following remain intentionally deferred because no pre-QA functional defect justifies their risk now:

- internal phone/wide duplication in `CharacterEquipmentClosureV4`;
- decomposition of the large `CharacterEditorScreenV4` orchestration surface;
- migration of the deprecated Kotlin Multiplatform `androidLibrary` target declaration to the newer target API. The compiler warning is real, but that change is a build-configuration migration rather than a local source cleanup and is not required for this QA candidate.

These are post-Phase-4 maintenance candidates, not blockers for M6.

## Guarded gates

### M5 cleanup + traceability gate

Workflow `33911204352` — **SUCCESS**.

It proved before accepting the cleanup tree:

- obsolete symbols had no external active references;
- all intended obsolete files/symbols were absent after cleanup;
- all six M4 repairs remained reachable;
- `CharacterOwnerLineageMigrationTest` remained present and `:shared:desktopTest` passed;
- Android debug assemble passed;
- Desktop build passed;
- backend dependency install/type-check passed.

Preparatory APK from that gate:

- SHA-256 `c676da4bfc3abd9ef157944615e25fb2cbef1a2657e659d5471382dad9c07867`;
- size `35,720,588` bytes.

This APK is technical evidence only and is not the final M6 candidate.

### Bounded warning cleanup gate

Workflow `33911629712` — **SUCCESS**.

It proved:

- exact four local warning repairs applied;
- full `:shared:desktopTest :androidApp:assembleDebug :desktopApp:build` regression passed;
- the four targeted Kotlin warning signatures were absent from compiler output;
- backend type-check passed without retaining a package lock;
- helper files self-deleted before accepted commit `b5145a1e0ae92bb7594b9b650f8849eb51f85ba2`.

### Clean standard repository gate

Workflow `33911956696` on exact commit `adc286b3e1305ed706c2ed04d478a43652f6b365` — **SUCCESS**.

It passed the ordinary repository gate across:

- backend Worker type-check;
- shared/Kotlin tests including the owner-lineage schema migration regression;
- Android debug assemble;
- Desktop build;
- APK upload.

Technical standard-gate artifact:

- artifact ID `9951791434`, name `dnd-custom-aid-debug-apk`;
- artifact ZIP digest `sha256:a09c1dc69f8f76d6d687923c99fe1900bb9ef6906cd6f71139ede47854cec9b4`.

### Independent exact frozen-candidate validation

Frozen branch `tmp/phase4-m5-frozen-qa-candidate` was created at exact commit `adc286b3e1305ed706c2ed04d478a43652f6b365` and was not modified afterward.

Validation ran from a separate helper branch and explicitly checked out the frozen SHA into a detached `candidate/` worktree. Therefore the validator workflow/helper is not part of the frozen source tree.

Workflow `33912322920` — **SUCCESS**.

Exact identity proven by the validator:

- candidate commit `adc286b3e1305ed706c2ed04d478a43652f6b365`;
- candidate tree `fd1f7feffde082b34cce41248e951a25eed7a004`;
- full shared/Kotlin test regression passed;
- Android debug assemble passed;
- Desktop build passed;
- backend type-check passed;
- exact extracted APK SHA-256 `e31ce44a84cd79260ea2c51c65cb6a63675b1f916998e44d583358d72893c8ee`;
- exact extracted APK size `35,720,588` bytes;
- artifact ID `9951922423`, name `phase4-m5-frozen-qa-apk`;
- artifact ZIP digest `sha256:5fb8d7f281dbf937def89db4377e9b4157c46343f07721912aa759bb52d6f9fa`;
- artifact ZIP size `12,354,957` bytes.

The historical L candidate remains immutable historical evidence only. The M5 frozen candidate above is the only intended candidate for M6.

## M5 closure

**Batch M5 is GREEN and complete.**

The next phase is **M6 owner real-device QA** on the exact frozen candidate above. No product-code change may be made to the frozen branch. Any QA blocker that requires production repair invalidates this candidate for affected QA evidence and requires a focused repair, full technical gates, a new exact frozen candidate identity, and repetition of affected owner QA.

`main` remains untouched pending the separately governed Phase 4 merge/closure decision after owner QA acceptance and final governance/merge-boundary housekeeping.
